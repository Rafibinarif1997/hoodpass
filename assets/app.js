const $=s=>document.querySelector(s);const wallet=()=>localStorage.getItem('wcc_wallet')||'';function short(a){return a?a.slice(0,6)+'…'+a.slice(-4):''}function toast(x){let t=$('#toast');if(t){t.textContent=x;t.style.display='block';setTimeout(()=>t.style.display='none',2200)}}async function connect(){if(!window.ethereum)return toast('Install an EVM wallet first.');try{let a=await ethereum.request({method:'eth_requestAccounts'});if(a[0]){localStorage.setItem('wcc_wallet',a[0]);location.reload()}}catch(e){toast('Connection cancelled.')}}function disconnect(){localStorage.removeItem('wcc_wallet');location.reload()}async function rpc(url,method,params){let r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method,params})});let j=await r.json();if(j.error)throw Error(j.error.message);return j.result}async function balances(){let box=$('#balances'),a=wallet();if(!box||!a)return;if(!a){box.innerHTML='<div class="empty">Connect wallet first.</div>';return}let cs=[['Ethereum','ETH','https://ethereum-rpc.publicnode.com'],['Base','ETH','https://base-rpc.publicnode.com'],['Arbitrum','ETH','https://arbitrum-one-rpc.publicnode.com'],['Optimism','ETH','https://optimism-rpc.publicnode.com'],['Polygon','POL','https://polygon-bor-rpc.publicnode.com'],['BNB Chain','BNB','https://bsc-rpc.publicnode.com']];box.innerHTML='<div class="empty">Loading live public-RPC balances…</div>';let rows=[];for(let c of cs){try{let h=await rpc(c[2],'eth_getBalance',[a,'latest']);let n=Number(BigInt(h))/1e18;rows.push(`<tr><td>${c[0]}</td><td>${n.toFixed(6)} ${c[1]}</td><td><span class="badge">LIVE</span></td></tr>`)}catch(e){rows.push(`<tr><td>${c[0]}</td><td>Unavailable</td><td>—</td></tr>`)}}box.innerHTML='<table><tr><th>Chain</th><th>Native balance</th><th>Status</th></tr>'+rows.join('')+'</table>'}document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-connect]').forEach(x=>x.onclick=connect);document.querySelectorAll('[data-disconnect]').forEach(x=>x.onclick=disconnect);document.querySelectorAll('[data-wallet]').forEach(x=>x.textContent=wallet()?short(wallet()):'Not connected');let m=$('#mobile'),s=$('#side');if(m)m.onclick=()=>s.classList.toggle('open');balances()});
