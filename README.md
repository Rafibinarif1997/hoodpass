# Web3 Command Center — Free MVP

GitHub Pages-ready multi-page prototype.

Working: EVM wallet connection and live native balances via public RPC endpoints.

The token/NFT/history/eligibility data layers are intentionally separated because reliable multi-chain indexing normally needs an API/indexer. The product structure is ready for those integrations.
