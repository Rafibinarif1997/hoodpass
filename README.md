# GOAL2WL
A static skill-based WL campaign website.

## Run
Open `index.html` in a browser. No npm required.

## Production
Replace localStorage claim logic with a server-side verification flow (e.g. Supabase Edge Function + database) before distributing real WL.
